#!/bin/bash

cat <<EOF
This is a S2I Rust 1.7 CentOS base image. To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

# inside the rust sti repository
$ docker build -t rust-sti 1.7/
$ s2i build https://github.com/kargakis/s2i-rust.git \\
    --context-dir=/1.7/test/test-app/ rust-s2i rust-test-app

You can then run the resulting image via:
$ docker run -p 8080:8080 rust-test-app
EOF
